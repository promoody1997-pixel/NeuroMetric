buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // تحديث إلى أحدث إصدار مستقر ومتوافق مع جافا 17
        classpath 'com.android.tools.build:gradle:8.3.2'
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
